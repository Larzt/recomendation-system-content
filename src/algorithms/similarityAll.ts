import { useFilesStore } from '@/stores'

/**
 * Calcula la matriz de similitud para todos los documentos del store.
 * Devuelve una matriz 2D (number[][]) donde la fila i y columna j
 * representan la similitud entre el documento i y el documento j
 * siguiendo el orden de `useFilesStore().documents`.
 */
export function similarityAll(): number[][] {

    
}

